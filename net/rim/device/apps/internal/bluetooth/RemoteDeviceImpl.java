package net.rim.device.apps.internal.bluetooth;

import javax.bluetooth.RemoteDevice;

final class RemoteDeviceImpl extends RemoteDevice {
   RemoteDeviceImpl(String address) {
      super(address);
   }
}
