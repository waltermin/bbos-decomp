package net.rim.device.apps.api.transmission;

final class DefaultSendThread$SendThreadDataContainer {
   private final Packet _packet;
   private final DefaultSendThread _profile;

   DefaultSendThread$SendThreadDataContainer(Packet p, DefaultSendThread t) {
      this._packet = p;
      this._profile = t;
   }
}
